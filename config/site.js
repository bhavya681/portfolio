/** Served from /public — Hire me in the navbar triggers download. */
export const RESUME_FILENAME = "bhavyaresumefullstack.pdf";

export const resumeDownload = {
  href: `/${RESUME_FILENAME}`,
  filename: RESUME_FILENAME,
};
